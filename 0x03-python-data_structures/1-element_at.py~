#!/usr/bin/python3
def element_at(my_list, idx):
    for x in my_list:
        if idx < 0:
            return
        elif idx > len(my_list):
            return
        else:
            return x[idx]
